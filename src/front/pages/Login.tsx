import { Link } from 'react-router-dom';
import { LoginContainer} from '@components';

function Login() {

  return (
    <LoginContainer>
      <h1>로그인</h1>
    </LoginContainer>
  );
}

export default Login;
